# --- Backend Server & API ---
fastapi>=0.109.2
uvicorn>=0.27.1
pydantic>=2.6.1
pydantic-settings>=2.0.0   # config.py 의 BaseSettings (누락 시 ImportError)
sse-starlette>=2.1.0

# --- RAG Core (LangChain & LangGraph) ---
langchain-core
langchain-community
langchain-text-splitters   # ingest/chunker.py 의 RecursiveCharacterTextSplitter
langgraph>=0.0.24
langgraph-checkpoint-sqlite
aiosqlite                  # AsyncSqliteSaver (비동기 그래프 실행에 필수)

# --- LLM Providers ---
langchain-google-genai>=1.0.0

# --- Vector DB (Pinecone) ---
pinecone>=5.0.0            # 인덱스 생성/삭제에 클라이언트를 직접 사용
langchain-pinecone         # PineconeVectorStore (dense 검색)

# --- Embeddings (BAAI/bge-m3 via HuggingFace) ---
sentence-transformers>=2.3.1
langchain-huggingface
safetensors
transformers
numpy
cryptography

# --- Data Ingestion & Search Tools ---
PyMuPDF>=1.23.21           # PDF 파싱
GitPython>=3.1.43          # GitHub 연동
PyGithub                   # GitHub 연동
beautifulsoup4             # 웹 크롤링 (WebBaseLoader 용)

# --- 기타 ---
requests                   # 이미지 다운로드(Vision 분석)
tenacity                   # 재시도 로직
